server {
		listen 80;
		# Redirige tout le HTTP vers HTTPS
		return 301 https://$host$request_uri;
}

# server {
#		listen 443 ssl;
#		server_name m[on-domaine.com](http://ton-domaine.com/);
#		root /var/www/html/public;
#		index index.php;
#
#		ssl_certificate /etc/letsencrypt/live/ton-domaine.com/fullchain.pem;
#		ssl_certificate_key /etc/letsencrypt/live/ton-domaine.com/privkey.pem;
#
#		location / {
#		    try_files $uri $uri/ /index.php?$query_string;
#		}
#
#		location ~ \\.php$ {
#		    fastcgi_pass app:9000;
#		    fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#		    include fastcgi_params;
#		}
#}
